# CommandParserService Architecture

## System Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         User Voice Input                             │
│                    "slack message: deploy done"                      │
└───────────────────────────────┬─────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      Whisper Transcription                           │
│                  (Existing OpenWhispr Flow)                          │
└───────────────────────────────┬─────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      CommandService.detectCommand()                  │
│                                                                       │
│  Step 1: Quick Pre-validation                                        │
│  ┌────────────────────────────────────────────────────────┐         │
│  │  CommandParserService.looksLikeSlackCommand(text)      │         │
│  │  • Checks for "slack", "send to slack" keywords        │         │
│  │  • Returns boolean (no AI call)                        │         │
│  └────────────────┬───────────────────────────────────────┘         │
│                   │                                                  │
│                   ├─ No ─→ Return { isCommand: false }              │
│                   │                                                  │
│                   └─ Yes ─→ Continue to AI Parsing                  │
│                                                                       │
└───────────────────────────────┬─────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│              CommandParserService.parseSlackCommand()                │
│                                                                       │
│  Step 2: AI-Powered Parsing                                          │
│  ┌────────────────────────────────────────────────────────┐         │
│  │  processWithCustomPrompt(input, prompt, model)         │         │
│  │                                                         │         │
│  │  1. Save original prompts from localStorage            │         │
│  │  2. Inject custom extraction prompt                    │         │
│  │  3. Call ReasoningService.processText()                │         │
│  │  4. Restore original prompts                           │         │
│  └────────────────┬───────────────────────────────────────┘         │
│                   │                                                  │
└───────────────────┼──────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     ReasoningService.processText()                   │
│                                                                       │
│  Step 3: Provider Routing                                            │
│  ┌────────────────────────────────────────────────────────┐         │
│  │  getModelProvider(model)                               │         │
│  │  • "gemini-*" → processWithGemini()                    │         │
│  │  • "gpt-*" → processWithOpenAI()                       │         │
│  │  • "claude-*" → processWithAnthropic()                 │         │
│  │  • "qwen-*" → processWithLocal()                       │         │
│  └────────────────┬───────────────────────────────────────┘         │
│                   │                                                  │
└───────────────────┼──────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                   Provider-Specific Processing                       │
│                                                                       │
│  Step 4: Construct API Request                                       │
│  ┌────────────────────────────────────────────────────────┐         │
│  │  For Gemini:                                           │         │
│  │  • systemPrompt: "You are a dictation assistant..."   │         │
│  │  • userPrompt: getReasoningPrompt(text, null, config) │         │
│  │                                                         │         │
│  │  getReasoningPrompt() reads from localStorage:        │         │
│  │  customPrompts.regular = "Extract Slack message..."   │         │
│  │  ├─ Replaces {{text}} with actual input               │         │
│  │  └─ Returns: "Extract... Voice: slack message:..."    │         │
│  └────────────────┬───────────────────────────────────────┘         │
│                   │                                                  │
└───────────────────┼──────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        AI Provider API                               │
│                                                                       │
│  Gemini Example Request:                                             │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  POST https://generativelanguage.googleapis.com/v1beta/... │   │
│  │  {                                                          │   │
│  │    "contents": [{                                           │   │
│  │      "parts": [{                                            │   │
│  │        "text": "You are a dictation assistant...\n\n        │   │
│  │                 Extract the Slack message content...\n      │   │
│  │                 Voice command: slack message: deploy done\n │   │
│  │                 ...\nExtracted message:"                    │   │
│  │      }]                                                      │   │
│  │    }],                                                       │   │
│  │    "generationConfig": {                                    │   │
│  │      "temperature": 0.1,                                    │   │
│  │      "maxOutputTokens": 500                                 │   │
│  │    }                                                         │   │
│  │  }                                                           │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                       │
└───────────────────────────────┬─────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          AI Response                                 │
│                                                                       │
│  Gemini Response:                                                    │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  {                                                          │   │
│  │    "candidates": [{                                         │   │
│  │      "content": {                                           │   │
│  │        "parts": [{                                          │   │
│  │          "text": "deploy done"                              │   │
│  │        }]                                                    │   │
│  │      }                                                       │   │
│  │    }]                                                        │   │
│  │  }                                                           │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                       │
└───────────────────────────────┬─────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     Response Processing                              │
│                                                                       │
│  Step 5: Extract Text from Response                                  │
│  ┌────────────────────────────────────────────────────────┐         │
│  │  ReasoningService returns: "deploy done"               │         │
│  │         ▼                                               │         │
│  │  CommandParserService validates & returns:             │         │
│  │  { message: "deploy done" }                            │         │
│  └────────────────────────────────────────────────────────┘         │
│                                                                       │
└───────────────────────────────┬─────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     CommandService.executeCommand()                  │
│                                                                       │
│  Step 6: Execute Slack Webhook                                       │
│  ┌────────────────────────────────────────────────────────┐         │
│  │  1. 5-second countdown (cancellable)                   │         │
│  │  2. Call electronAPI.executeSlackWebhook()             │         │
│  │  3. POST to Slack webhook URL                          │         │
│  └────────────────────────────────────────────────────────┘         │
│                                                                       │
└───────────────────────────────┬─────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          Slack Message Sent                          │
│                       "deploy done"                                  │
└─────────────────────────────────────────────────────────────────────┘
```

## Component Interaction

```
┌──────────────────────────┐
│   CommandService.ts      │
│   (Orchestrator)         │
│                          │
│  • detectCommand()       │◄──── Entry point from transcription
│  • executeCommand()      │
└────────┬─────────────────┘
         │
         │ calls
         ▼
┌──────────────────────────┐
│ CommandParserService.ts  │
│ (AI Parser)              │
│                          │
│  • looksLikeSlackCommand()    Quick validation (no API cost)
│  • parseSlackCommand()   │◄─── AI-powered extraction
│  • processWithCustomPrompt()  Internal helper
└────────┬─────────────────┘
         │
         │ uses
         ▼
┌──────────────────────────┐
│  ReasoningService.ts     │
│  (AI Infrastructure)     │
│                          │
│  • processText()         │◄─── Main entry point
│  • processWithGemini()   │     Provider routing
│  • processWithOpenAI()   │     API key management
│  • processWithAnthropic()│     Error handling
│  • processWithLocal()    │     Retry logic
└────────┬─────────────────┘
         │
         │ uses
         ▼
┌──────────────────────────┐
│ BaseReasoningService.ts  │
│ (Base Class)             │
│                          │
│  • getReasoningPrompt()  │◄─── Reads localStorage for custom prompts
│  • calculateMaxTokens()  │     Token limit calculation
└──────────────────────────┘
```

## Data Flow: Prompt Template System

```
1. Custom Prompt Storage (CommandParserService)
   ┌─────────────────────────────────────────────────────────┐
   │ const SLACK_EXTRACTION_PROMPT =                         │
   │   "Extract the Slack message content...\n              │
   │    Voice command: {{text}}\n                           │
   │    ...\n                                                │
   │    Extracted message:"                                  │
   └─────────────────────────────────────────────────────────┘
                      │
                      ▼
2. Temporary Injection (processWithCustomPrompt)
   ┌─────────────────────────────────────────────────────────┐
   │ localStorage.setItem('customPrompts', JSON.stringify({  │
   │   regular: SLACK_EXTRACTION_PROMPT,                     │
   │   agent: SLACK_EXTRACTION_PROMPT                        │
   │ }))                                                      │
   └─────────────────────────────────────────────────────────┘
                      │
                      ▼
3. Template Processing (BaseReasoningService.getReasoningPrompt)
   ┌─────────────────────────────────────────────────────────┐
   │ const customPrompts = localStorage.getItem(...)         │
   │ const template = customPrompts.regular                  │
   │ return template.replace(/\{\{text\}\}/g, actualInput)  │
   └─────────────────────────────────────────────────────────┘
                      │
                      ▼
4. Final Prompt Sent to AI
   ┌─────────────────────────────────────────────────────────┐
   │ "Extract the Slack message content...\n                │
   │  Voice command: slack message: deploy done\n           │
   │  ...\n                                                  │
   │  Extracted message:"                                    │
   └─────────────────────────────────────────────────────────┘
                      │
                      ▼
5. AI Response
   ┌─────────────────────────────────────────────────────────┐
   │ "deploy done"                                           │
   └─────────────────────────────────────────────────────────┘
                      │
                      ▼
6. Cleanup (processWithCustomPrompt finally block)
   ┌─────────────────────────────────────────────────────────┐
   │ localStorage.setItem('customPrompts', originalPrompts)  │
   └─────────────────────────────────────────────────────────┘
```

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────┐
│  CommandParserService.parseSlackCommand()               │
└────┬────────────────────────────────────────────────────┘
     │
     │ try
     ▼
┌─────────────────────────────────────────────────────────┐
│  processWithCustomPrompt()                              │
└────┬────────────────────────────────────────────────────┘
     │
     │ try
     ▼
┌─────────────────────────────────────────────────────────┐
│  ReasoningService.processText()                         │
└────┬────────────────────────────────────────────────────┘
     │
     ├─────► Error: API key missing
     │       └─→ throw "Gemini API key not configured"
     │
     ├─────► Error: Network failure
     │       └─→ withRetry() attempts (3 retries)
     │           └─→ throw "Gemini API error: 500"
     │
     ├─────► Error: Rate limit
     │       └─→ throw "Gemini API error: 429"
     │
     └─────► Success: Returns "deploy done"
             │
             ▼
     ┌─────────────────────────────────────────────┐
     │  Validate response                          │
     │  • Check if empty                           │
     │  • Trim whitespace                          │
     └─────┬───────────────────────────────────────┘
           │
           ├─────► Empty response
           │       └─→ throw "Could not extract message"
           │
           └─────► Valid response
                   └─→ return { message: "deploy done" }

Fallback Strategy in CommandService.detectCommand():
┌─────────────────────────────────────────────────────────┐
│  try {                                                  │
│    const result = await CommandParserService...        │
│    return result;                                       │
│  } catch (error) {                                      │
│    // Fall back to regex pattern                       │
│    const match = text.match(SLACK_MESSAGE_PATTERN);   │
│    if (match) return { isCommand: true, ... };        │
│    return { isCommand: true, error: "..." };          │
│  }                                                      │
└─────────────────────────────────────────────────────────┘
```

## Performance Optimization Flow

```
Input: User transcription
   │
   ▼
┌────────────────────────────────────────────────┐
│ Quick Keyword Check (0ms, no cost)             │
│ CommandParserService.looksLikeSlackCommand()   │
└────┬───────────────────────────────────────────┘
     │
     ├─── No "slack" keywords found
     │    └─→ Skip AI parsing (save $0.005)
     │        return { isCommand: false }
     │
     └─── Has "slack" keywords
          │
          ▼
     ┌────────────────────────────────────────────┐
     │ AI Parsing (200-500ms, ~$0.000005)        │
     │ CommandParserService.parseSlackCommand()  │
     └────┬───────────────────────────────────────┘
          │
          ├─── Success
          │    └─→ return { message: "..." }
          │
          └─── Error
               └─→ Fallback to regex (0ms)
                   return regex match or error

Cost Analysis:
• Keyword check: Free, instant
• AI parsing: ~$0.000005 per call
• Regex fallback: Free, instant

Typical Usage (1000 voice commands):
• 900 normal dictation (no Slack keywords)
  └─→ 900 × $0 = $0 (skipped via keyword check)
• 100 Slack commands
  ├─→ 95 successful AI parses × $0.000005 = $0.000475
  └─→ 5 failed AI → regex fallback × $0 = $0
• Total cost: ~$0.0005 per 1000 commands
```

## Model Selection Strategy

```
┌─────────────────────────────────────────────────────────┐
│                  Model Selection Tree                   │
└─────────────────────────────────────────────────────────┘

              CommandParserService.parseSlackCommand()
                            │
                            │ model parameter
                            ▼
              ┌─────────────────────────────┐
              │  getModelProvider(model)    │
              └───────┬─────────────────────┘
                      │
        ┌─────────────┼─────────────┬─────────────┐
        │             │             │             │
        ▼             ▼             ▼             ▼
   ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐
   │ Gemini  │  │ OpenAI  │  │Anthropic│  │  Local  │
   └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘
        │            │            │            │
        │            │            │            │
        ▼            ▼            ▼            ▼

Gemini Models:
• gemini-2.5-flash-lite (default)
  ├─ Speed: ⚡⚡⚡⚡⚡ (200ms)
  ├─ Cost: 💰 ($0.000005)
  └─ Quality: ⭐⭐⭐⭐ (95% accuracy)

• gemini-2.5-flash
  ├─ Speed: ⚡⚡⚡⚡ (300ms)
  ├─ Cost: 💰💰 ($0.00001)
  └─ Quality: ⭐⭐⭐⭐⭐ (98% accuracy)

• gemini-2.5-pro
  ├─ Speed: ⚡⚡⚡ (500ms)
  ├─ Cost: 💰💰💰 ($0.00005)
  └─ Quality: ⭐⭐⭐⭐⭐ (99% accuracy)

OpenAI Models:
• gpt-4o-mini
  ├─ Speed: ⚡⚡⚡⚡ (300ms)
  ├─ Cost: 💰💰 ($0.00001)
  └─ Quality: ⭐⭐⭐⭐⭐ (98% accuracy)

• gpt-4o
  ├─ Speed: ⚡⚡⚡ (600ms)
  ├─ Cost: 💰💰💰💰 ($0.0001)
  └─ Quality: ⭐⭐⭐⭐⭐ (99.5% accuracy)

Anthropic Models:
• claude-3-5-haiku-20241022
  ├─ Speed: ⚡⚡⚡⚡ (400ms)
  ├─ Cost: 💰💰 ($0.00002)
  └─ Quality: ⭐⭐⭐⭐⭐ (98% accuracy)

• claude-3-5-sonnet-20241022
  ├─ Speed: ⚡⚡⚡ (800ms)
  ├─ Cost: 💰💰💰💰 ($0.0001)
  └─ Quality: ⭐⭐⭐⭐⭐ (99.5% accuracy)

Recommendation Matrix:

Use Case                    │ Recommended Model
────────────────────────────┼──────────────────────────
Production (default)        │ gemini-2.5-flash-lite
High volume, cost-sensitive │ gemini-2.5-flash-lite
Complex parsing needed      │ gpt-4o-mini / gemini-2.5-flash
Maximum accuracy needed     │ gpt-4o / claude-3-5-sonnet
Offline/privacy required    │ Local models (qwen)
```

## Scalability Considerations

```
Current Architecture (Single User):
┌──────────────────────────────────────────────┐
│  User speaks → AI parses → Slack sends       │
│  Latency: 200-800ms                          │
│  Cost: $0.000005-0.0001 per command          │
└──────────────────────────────────────────────┘

Future: Multi-User / High Volume:
┌──────────────────────────────────────────────┐
│  Optimization Strategies                     │
├──────────────────────────────────────────────┤
│  1. Caching Layer                            │
│     ├─ Cache common commands                 │
│     ├─ Fuzzy match similar inputs            │
│     └─ Reduce API calls by 60-80%            │
│                                              │
│  2. Batch Processing                         │
│     ├─ Queue multiple commands               │
│     ├─ Process in batch API call             │
│     └─ Reduce latency by 30%                 │
│                                              │
│  3. Smart Routing                            │
│     ├─ Simple commands → Regex               │
│     ├─ Moderate → Lite model                 │
│     └─ Complex → Full model                  │
│                                              │
│  4. Predictive Pre-parsing                   │
│     ├─ Detect "slack" during transcription   │
│     ├─ Start AI call early                   │
│     └─ Reduce perceived latency by 50%       │
└──────────────────────────────────────────────┘
```

## Extension Points

```
Adding New Command Types:

1. Create new parser method:
   CommandParserService.parseEmailCommand()
   CommandParserService.parseCalendarCommand()

2. Add detection in CommandService:
   if (CommandParserService.looksLikeEmailCommand(text)) {
     // Parse email command
   }

3. Use same infrastructure:
   • processWithCustomPrompt()
   • ReasoningService
   • Same error handling
   • Same model support

Template:
┌──────────────────────────────────────────────┐
│ static async parseXyzCommand(               │
│   userInput: string,                         │
│   model: string = 'gemini-2.5-flash-lite'   │
│ ): Promise<XyzCommandResult> {              │
│   const prompt = `Extract XYZ parameters..  │
│                   Input: {{text}}           │
│                   Parameters:`;             │
│                                              │
│   const result = await this                 │
│     .processWithCustomPrompt(               │
│       userInput, prompt, model              │
│     );                                       │
│                                              │
│   return parseJsonResponse(result);         │
│ }                                            │
└──────────────────────────────────────────────┘
```
